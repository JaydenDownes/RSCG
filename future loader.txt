To display a progress bar or percentage in the terminal instead of the individual file saving messages, you can use the tqdm library in Python. Here's how you can modify your code to incorporate a progress bar:

First, you need to install tqdm if you haven't already:

bash
Copy code
pip install tqdm
Then, you can modify your code like this:

python
Copy code
from tqdm import tqdm

# Your existing code for generating audio files goes here...

# Modify the loop where you save the audio files to use tqdm
for item, i in tqdm(zip(content, range(0, len(content))), desc='Generating audio files', total=len(content)):
    filename = f"outputs/temp_{post['id']}_{i}.mp3"
    tts(item, "en_us_001", filename, 1.15)
    dur = get_duration(filename)
    script.append((item, dur))

# Continue with your remaining code...
This will create a progress bar in the terminal showing the progress of generating audio files. You can customize the appearance of the progress bar by adjusting the parameters passed to tqdm, such as desc for the description displayed next to the progress bar and total for the total number of iterations.





